name: Update Health News

on:
  workflow_dispatch:
  schedule:
    # 08:00 Asia/Bangkok = 01:00 UTC
    - cron: "0 1 * * *"

permissions:
  contents: write

concurrency:
  group: health-news-update
  cancel-in-progress: false

jobs:
  fetch-news:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # Optional: only installs if you add requirements.txt later
      - name: Install dependencies (if requirements.txt exists)
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run news fetcher
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          python fetch_news.py

      - name: Commit and push data.json (rebase-safe)
        run: |
          set -e

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add data.json

          # If nothing changed, stop cleanly
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "Update health news $(date -u +'%Y-%m-%d %H:%M UTC')"

          # Prevent 'fetch first' push rejection
          git pull --rebase origin main

          git push origin main
